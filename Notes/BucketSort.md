Bucket Sort:

Ask yourself:
- “Do I need to group elements by a count, frequency, or range?”
- “Would sorting be too slow?”
- “Can I use array index to represent a property (like freq or range)?”

If yes → you can probably solve it with a bucket pattern instead of a heap or full sort.